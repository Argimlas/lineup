# Cookie/consent banner update

- [ ] 1. Add `forget()` to `src/hooks/useConsent.ts`
  - Removes the `consent` localStorage key and sets state back to `null`. Does not touch any other storage.
  - Success: hook returns `{ consent, accept, decline, forget }`; calling `forget()` clears only the `consent` key.

- [ ] 2. Footer "Privacy settings" button just re-opens the banner
  - In `App.tsx`, replace `handlePrivacyReset` (drop the `window.confirm` and the manual key removal + reload) with a handler that calls `forget()`.
  - Success: clicking "Privacy settings" in the running app shows the consent banner again immediately, with no confirm dialog, and any existing festival data is still visible/intact underneath.

- [ ] 3. Banner's "I decline" wipes everything, no popup
  - Update the decline button's `onClick` to: `localStorage.clear()` → `decline()` → `window.location.reload()` (this order matters: `decline()` must write the fresh `consent=declined` record *after* the clear, or the reload would show the banner again immediately).
  - Remove the now-unused `clearStorage` call/destructure from `App.tsx`, and delete `clearStorage` from `useLineup.ts` since nothing calls it anymore.
  - Success: clicking decline empties `localStorage` (only `consent: declined` remains after reload) and the app comes back up in a fresh, empty state.

- [ ] 4. Don't read festival data from storage while consent is null/declined
  - In `useFestivals.ts` and `useLineup.ts`, change the lazy `useState` initializers to only call `loadIndex()`/`loadFromStorage()`/`pickActiveFestival()` when `consented` is already `true` at mount; otherwise start from defaults.
  - Add a render-time "hydrate once" branch (same synchronous-during-render pattern the file already uses for cache misses) that fires the first time `consented` flips from `false` to `true`, so a user who accepts *after* seeing the banner (fresh visit, post-12-month-expiry, or after reopening via Privacy settings) still recovers previously saved data — without needing a page reload.
  - Apply the same `consented` guard to `setFestivalFor`'s fallback read in `useLineup.ts`.
  - Success: with `consented=false`, no `lineup_*`/`festival_index` reads happen; when `consented` later becomes `true`, previously stored data reappears in the UI without a reload.

- [ ] 5. Fix mobile button overflow
  - In `App.css`, inside the `@media (max-width: 640px)` block for `.consent-banner`, stack children vertically (`flex-direction: column`) and make `.consent-actions` and its buttons full width, so button text can't force a horizontal overflow past the screen edge.
  - Success: at ~375px viewport width, the banner and both buttons render fully on-screen with no horizontal clipping/overflow.

- [ ] 6. Update banner body copy
  - Reflect that declining now deletes everything immediately (not "when you close the tab"), staying within the easy-language rules (~15 words/sentence, plain vocabulary).
  - Success: copy is accurate to the new behavior and still reads in under ~10 seconds.
