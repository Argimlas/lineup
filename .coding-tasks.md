# Task Plan: fix standards-review findings (1, 2, 4)

Source: standards review of `main...HEAD` (feat/festival-tabs), findings #1, #2, #4.
Finding #3 (API naming sprawl) is skipped — cosmetic rename with cross-file
blast radius, not necessary.

- [x] **Task 1 — extract `safeStorage` helper**
  Add `src/lib/storage.ts` with `safeGet<T>(key, fallback): T`,
  `safeSet(key, value): void`, `safeRemove(key): void`, each wrapping the
  existing `try/catch { /* ignore */ }` shape.
  Replace the ~7 hand-rolled try/catch localStorage blocks in
  `src/hooks/useLineup.ts` and `src/hooks/useFestivals.ts` with calls to
  these helpers. No behavior change.
  **Done when:** `bun run build` and `bun run lint` pass, and a manual grep
  shows no remaining raw `localStorage.` calls in the two hook files.

- [x] **Task 2 — extract shared `useHydrateOnConsent` hook**
  Add `src/hooks/useHydrateOnConsent.ts`: `useHydrateOnConsent(consented:
  boolean, hydrate: () => void)`, encapsulating the `hydrated` state +
  `if (consented && !hydrated) { setHydrated(true); hydrate(); }` shape.
  Use it in `useLineup.ts` (replacing lines 36, 42-45) and `useFestivals.ts`
  (replacing lines 64, 66-71). No behavior change.
  **Done when:** `bun run build` and `bun run lint` pass; both hooks still
  hydrate from storage the first render after `consented` flips true.

- [ ] **Task 3 — extract `syncName` helper in `App.tsx`**
  Replace the duplicated `setFestival`/`replaceFestival` wrapper bodies
  (`App.tsx:24-32`) with a single `syncName(f)` helper called from both,
  removing the repeated `if (f?.name) renameFestival(activeId, f.name)`.
  No behavior change.
  **Done when:** `bun run build` and `bun run lint` pass.
