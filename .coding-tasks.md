# Break duration formatting in Timeline

- [ ] Add `formatDuration(minutes)` to `src/lib/time.ts`: minutes < 60 → `"45 min"`; exact hours → `"2h"`; otherwise → `"1h 30 min"`.
- [ ] Use `formatDuration` in `src/components/Timeline.tsx:195` instead of the inline `{gapEnd - gapStart} min`.
